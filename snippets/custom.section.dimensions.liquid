{%- liquid
  assign collection = section.settings.collection
  assign bundle_product_list = section.settings.bundle_product_list
  assign tolerance = section.settings.tolerance_percent
-%}

<dimensions-finder>
dimensions page

{%  for product in collections[collections].products %}
  {{ product.title }}--{{ product.selected_or_first_available_variant.sku }}
{% endfor %}

{{ collection }}--{{ bundle_product_list }}--{{ tolerance }}
</dimensions-finder>
