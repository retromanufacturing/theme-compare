{%- comment -%}
  Renders a tab block

  Accepts:
  - block {block} - Block object
  - product {product} - Product object
  - title {string} - Title of the tab
  - tab_content {string} - Content of the tab
  - page_content {string} - Content of the page
{%- endcomment -%}

{%- liquid
  assign product = section.settings.product | default: product
  assign title = title | default: block.settings.title
  assign tab_content = tab_content | default: block.settings.content
  assign page_content = page_content | default: block.settings.page.content

  case title
    when 'Manuals, manuals'
      assign tab_content = 'product manuals'
      echo tab_content
    when 'Specifications, specifications'
      assign tab_content = 'specifications here'
      echo tab_content
  endcase

  capture accordion_content
    echo tab_content
    echo page_content
  endcapture
-%}

{{ title }}

{%- if title != blank and accordion_content != blank -%}
  <div {{ block.shopify_attributes }}>
    {%- render 'element.accordion', title: title, slot: accordion_content -%}
  </div>
{%- endif -%}