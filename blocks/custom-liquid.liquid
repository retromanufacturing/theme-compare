{%- liquid
  if block.settings.custom_liquid != blank
    echo block.settings.custom_liquid
  endif
-%}

{% schema %}
{
  "name": "Custom Liquid",
  "tag": null,
  "settings": [
    {
      "type": "liquid",
      "id": "custom_liquid",
      "label": "Custom Liquid",
      "info": "Custom Liquid"
    }
  ],
  "presets": [
    {
      "name": "Custom Liquid",
      "category": "Custom"
    }
  ]
}
{% endschema %}
