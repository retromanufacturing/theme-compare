{% render 'block.product-features', product: closest.product %}

{% schema %}
{
  "name": "Product features",
  "settings": [
    {
      "type": "range",
      "id": "font_size",
      "label": "t:custom.font_size",
      "default": 14,
      "min": 8,
      "max": 60,
      "unit": "px"
    }
  ],
  "presets": [{ "name": "Product features" }]
}
{% endschema %}
