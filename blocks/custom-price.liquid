<span class="block-price--{{ block.id }}">
  {% render 'block.product-price' %}
</span>

{% style %}
  .block-price--{{ block.id }} {
    color: {{ block.settings.color }};

    .element-text {
        display: none;
    }
  }
{% endstyle %}

{% schema %}
{
  "name": "t:labels.price",
  "settings": [
    {
      "type": "checkbox",
      "id": "hide_subtitle",
      "label": "Hide price subtitle",
      "default": false
    },
    {
      "type": "color",
      "id": "color",
      "label": "Color",
      "default": " #000"
    }
  ],
  "presets": [{ "name": "t:labels.price" }]
}
{% endschema %}
